const Welcome = ({ title, name }) => {
    return (
        <div className="welcome-guest">
            <h1>Welcome {title} {name} to Ammar's Website</h1>
        </div>
    );
}
 
export default Welcome;